const router = require("express").Router();

// Update user
  router.put("/:id", async (req, res) => {
          
  });

// Delete user

// Get a user

// Follow a user

// Unfollow a user

module.exports = router;